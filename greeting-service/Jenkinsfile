pipeline{
    agent any

    stages{
        stage('Deploy') {
            steps {
                sh '''
                    oc project student-deploy-strategies
                    oc start-build greeting-service --follow --wait
                '''
            }
        }
    }
}
